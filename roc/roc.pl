#!/usr/bin/perl -w 
#hupili
#20111217

#draw the roc curve for single features. 
#input the weka compatible .arff file. 

use strict ;

my $usage = "$0 {fn_arff}" ;

my $ARGC = @ARGV ;
($ARGC == 1) or die("usage:$usage\n") ;


exit 0 ;
